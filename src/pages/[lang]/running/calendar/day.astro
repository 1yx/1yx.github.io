---
import Layout from "@/layouts/Layout.astro";
import Header from "@/components/Header.astro";
import Footer from "@/components/Footer.astro";
import Breadcrumb from "@/components/Breadcrumb.astro";
import DayViewApp from "@/components/training/DayViewApp.astro";
import { SITE } from "@/config";
import { LOCALES, DEFAULT_LOCALE, t, type Locale } from "@/i18n";

export async function getStaticPaths() {
  return LOCALES.filter(l => l !== DEFAULT_LOCALE).map(lang => ({
    params: { lang },
  }));
}

const { lang } = Astro.params as { lang: Locale };
const ui = t(lang).training;
---

<Layout
  title={`${ui.dayTitle} | ${SITE.title}`}
  description={ui.dayDesc}
  lang={lang}
  alternateLanguages={[
    { lang: "zh-CN", href: `${SITE.website}running/calendar/day/` },
    { lang: "en", href: `${SITE.website}en/running/calendar/day/` },
  ]}
>
  <Header {lang} />
  <Breadcrumb {lang} />
  <main id="main-content" class="app-layout">
    <DayViewApp {lang} />
  </main>
  <Footer {lang} />
</Layout>
